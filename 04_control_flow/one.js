// switch(key){
//     case value:
//         break;
//     default:
//         break;
// }

// if(condition){}

//Falsy values = false, "", 0, 0n(bigint), null, undefined, NaN(not a number)
//Truthy values = "0", "false", "  ", [], {} , function(){} ( empty function)

// in case of array never check like this ' if(array){};
// if(Array.lenghth === 0) console.log("Array is empty ");

// if(Object.keys(emptyObject).length === 0){}   // .keys will return array of keys

// Nullish Coalescing Operator(??): null undefined
    // this operator is just made to check if null or undefined value is comming it will insert other value to variable

// let var1 = null??undefined??12;
// console.log(var1);

// var1 = 1??12;
// console.log(var1);


// Ternary operator  : condition ? true: false;
